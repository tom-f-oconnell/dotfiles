#!/usr/bin/env python3

from setuptools import setup, find_packages

setup(
    name='',
    version='0.0.1',
    install_requires=['pandas'],
    packages=find_packages(),
    #scripts=['scripts/.py'],
)
