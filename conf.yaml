- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link: 
    ~/.bashrc:
    ~/.bash_aliases:
    ~/.variables:
    ~/.profile:
    ~/.vimrc:
    ~/.vim:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.config/LibreCAD:
    ~/.config/inkscape:

# TODO is this git update-submodules necessary? how different from one in ./install?
- shell: []
#  - git update-submodules
