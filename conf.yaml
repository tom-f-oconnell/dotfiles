- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link: 
    ~/.bashrc:
    ~/.bash_aliases:
    ~/.variables:
    ~/.profile:
    ~/.vimrc:
    ~/.vim:
    ~/.gitconfig:
    ~/.gitignore:

# TODO is this git update-submodules necessary? how different from one in ./install?
- shell: []
#  - git update-submodules
