[user]
	email = toconnel@caltech.edu
    name = Tom O'Connell

[core]
    excludesfile = ~/.gitignore

# This works for both pull and push. No need to set upstream when first setting
# up.
# https://stackoverflow.com/questions/658885
[branch "master"]
	merge = refs/heads/master
	remote = origin
